extends layout

block content
  h1= title
  p Welcome to #{title}
  if messages.length
    ul
      each message in messages
        li
          p User: #{message.user}
          p Text: #{message.text}
          p Date: #{message.added}

  a(href="/new") Add message 